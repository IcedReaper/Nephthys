interface {
    public void function resize(required string  source, required numeric width, required string  target, required boolean overwrite, required numeric height, required string  interpolation, required numeric quality);
}